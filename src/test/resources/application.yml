jwt:
  secret: testSecret
cloud:
  aws:
    s3:
      bucket: test-bucket
    credentials:
      access-key: test-access-key
      secret-key: test-secret-key
    region:
      static: ap-northeast-2
    stack:
      auto: false
firebase:
  url: https://fcm.googleapis.com/v1/projects/testproject/messages:send
management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: "/base-path"
  metrics:
    tags:
      application: sw-contest